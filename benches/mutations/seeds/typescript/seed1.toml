# Mutation metadata for seed1.ts (DECISIONS.md D6: curated per seed, only
# unit-local identifiers renamed — external names are kept and must not drift).
[rename]
summarizeScores = "collectReport"
scores = "items"
threshold = "cutoff"
summaries = "lines"
runningTotal = "acc"
score = "points"
grade = "band"

[literals]
# non-keep-list literals only (D6b); 0 is keep-list and untouched
"90" = "85"
"50" = "40"
"250" = "400"
'"excellent"' = '"stellar"'
'"poor"' = '"weak"'
'"adequate"' = '"fine"'
'"aggregate"' = '"summary"'
